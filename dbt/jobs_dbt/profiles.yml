jobs_dbt:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: "{{ env_var('DBT_PG_PASSWORD') }}"
      port: 5432
      dbname: airflow
      schema: public
      threads: 4
